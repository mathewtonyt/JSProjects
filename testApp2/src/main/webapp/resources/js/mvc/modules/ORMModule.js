//the file will be used as a module in the other classes

var ORMModule = {
	
	//	this is the shared function that will be used
	exists : function(id) {console.log("checking the existence of the class")}
}